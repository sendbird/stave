import * as stylex from "@stylexjs/stylex";
import { vars } from "../ads/tokens/tokens.stylex";

export const resultStyles = stylex.create({
  panel: {
    minWidth: 0,
    backgroundColor: vars.colorCanvas,
    padding: vars.space12,
  },
  heading: {
    marginBottom: vars.space8,
    fontSize: vars.fontSizeBody,
    lineHeight: vars.lineHeightNormal,
    fontWeight: vars.fontWeightMedium,
  },
  body: { minWidth: 0 },
  introduction: {
    maxWidth: "65ch",
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorTextMuted,
  },
  navigation: {
    marginTop: vars.space12,
    display: "flex",
    flexWrap: "wrap",
    gap: vars.space8,
  },
  guidance: {
    marginTop: vars.space8,
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorTextMuted,
  },
  loading: { paddingBlock: vars.space8, fontSize: vars.fontSizeCaption },
  error: {
    paddingBlock: vars.space8,
    fontSize: vars.fontSizeBody,
    lineHeight: vars.lineHeightNormal,
    color: vars.colorDangerText,
  },
  empty: {
    paddingBlock: vars.space12,
    fontSize: vars.fontSizeBody,
    lineHeight: vars.lineHeightNormal,
    color: vars.colorTextMuted,
  },
  notice: {
    paddingBlock: vars.space8,
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorTextMuted,
  },
  pagination: {
    display: "flex",
    flexWrap: "wrap",
    alignItems: "center",
    justifyContent: "space-between",
    gap: vars.space8,
    borderTopWidth: 1,
    borderTopStyle: "solid",
    borderTopColor: vars.colorBorder,
    paddingTop: vars.space8,
  },
  caption: {
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorTextMuted,
  },
  list: { listStyleType: "none", margin: 0, padding: 0 },
  row: {
    display: "flex",
    flexDirection: "column",
    gap: vars.space8,
    paddingBlock: vars.space12,
    borderTopWidth: { default: 1, ":first-child": 0 },
    borderTopStyle: "solid",
    borderTopColor: vars.colorBorder,
  },
  rowHeader: {
    display: "flex",
    flexWrap: "wrap",
    alignItems: "center",
    justifyContent: "space-between",
    gap: vars.space8,
  },
  status: {
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    fontWeight: vars.fontWeightMedium,
    color: vars.colorText,
  },
  reviewState: {
    fontWeight: vars.fontWeightRegular,
    color: vars.colorTextMuted,
  },
  timestamp: {
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    fontVariantNumeric: "tabular-nums",
    color: vars.colorTextMuted,
  },
  summary: {
    whiteSpace: "pre-wrap",
    overflowWrap: "break-word",
    fontSize: vars.fontSizeBody,
    lineHeight: vars.lineHeightNormal,
    color: vars.colorText,
  },
  evidence: {
    display: "flex",
    flexDirection: "column",
    gap: vars.space8,
    paddingBlock: vars.space12,
    fontSize: vars.fontSizeCaption,
  },
  evidenceHeading: { fontWeight: vars.fontWeightMedium },
  evidenceDescription: { marginBlock: vars.space8, color: vars.colorTextMuted },
  answer: {
    whiteSpace: "pre-wrap",
    overflowWrap: "break-word",
    lineHeight: vars.lineHeightRelaxed,
  },
  excerptNotice: { marginTop: vars.space8, color: vars.colorTextMuted },
  files: {
    marginTop: vars.space12,
    display: "flex",
    flexDirection: "column",
    gap: vars.space4,
  },
  filePath: { wordBreak: "break-all", fontFamily: vars.fontMono },
  muted: { color: vars.colorTextMuted },
  reference: {
    minWidth: 0,
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorTextMuted,
  },
  disclosure: { cursor: "pointer", borderRadius: vars.radiusMark },
  resolution: { marginBottom: vars.space12 },
  runId: { wordBreak: "break-all", paddingBlock: vars.space4 },
  messageId: { wordBreak: "break-all" },
  snapshot: {
    minWidth: 0,
    borderTopWidth: 1,
    borderTopStyle: "solid",
    borderTopColor: vars.colorBorder,
    paddingBlock: vars.space8,
  },
  snapshotSummary: {
    cursor: "pointer",
    wordBreak: "break-all",
    borderRadius: vars.radiusMark,
    paddingBlock: vars.space4,
    fontFamily: vars.fontMono,
  },
  snapshotContent: {
    display: "flex",
    flexDirection: "column",
    gap: vars.space12,
    paddingTop: vars.space8,
  },
  snapshotSide: { display: "flex", flexDirection: "column", gap: vars.space4 },
  code: {
    maxHeight: "12rem",
    overflow: "auto",
    whiteSpace: "pre-wrap",
    wordBreak: "break-all",
    borderRadius: vars.radiusControl,
    backgroundColor: vars.colorSurfaceTint,
    padding: vars.space8,
    fontFamily: vars.fontMono,
    fontSize: vars.fontSizeCaption,
    lineHeight: vars.lineHeightTight,
    color: vars.colorText,
  },
  snapshots: { marginTop: vars.space12, minWidth: 0 },
  snapshotsHeading: {
    marginBottom: vars.space4,
    fontWeight: vars.fontWeightMedium,
  },
  snapshotsDescription: {
    marginBottom: vars.space8,
    color: vars.colorTextMuted,
  },
  uncaptured: {
    wordBreak: "break-all",
    borderTopWidth: 1,
    borderTopStyle: "solid",
    borderTopColor: vars.colorBorder,
    paddingBlock: vars.space8,
  },
  mono: { fontFamily: vars.fontMono },
  modelFacts: {
    display: "grid",
    gridTemplateColumns: "auto minmax(0, 1fr)",
    columnGap: vars.space12,
    rowGap: 6,
    fontSize: vars.fontSizeCaption,
  },
  modelValue: {
    minWidth: 0,
    overflowWrap: "break-word",
    color: vars.colorText,
  },
  modelSource: { color: vars.colorText },
  modelReason: {
    whiteSpace: "pre-wrap",
    overflowWrap: "break-word",
    color: vars.colorText,
  },
});
